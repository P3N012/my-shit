# FastAPI and ASGI server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Authentication & Security
python-jose[cryptography]==3.3.0
# passlib 1.7.4 reads bcrypt.__about__.__version__, which was removed in
# bcrypt 4.x — pin to 3.x until passlib releases 1.8.
passlib[bcrypt]==1.7.4
bcrypt<4.0.0
# Fernet symmetric encryption for OAuth tokens at rest (app/core/crypto.py).
cryptography==48.0.0
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# AI substrate
anthropic==0.45.0
arq==0.26.1
redis==5.2.1

# Stripe Connect (revenue data source)
stripe==11.4.1

# Pydantic + EmailStr validator
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Utilities
python-dateutil==2.9.0

# Development (optional)
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1